# New UI targets introduced by the "ios-clean-ui" spec.
# Approved deltas are generated into docs/harness/specs/ui-map.yaml by:
#   fvm dart run tool/harness.dart spec ui-map
# This restyle reuses the existing directory-git-sync semantics identifiers
# (directory_sync_screen, directory_picker_button, selected_directory_text,
# remote_url_field, sync_button, sync_status_text,
# sync_success_text, sync_error_text) and introduces no new UI targets.
targets: {}
